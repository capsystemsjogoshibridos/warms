window.EMOJI_WARS_CONFIG = {
    // Deixe vazio para tentar conectar no mesmo domínio.
    // Se o frontend estiver na Vercel e o server em outro host, coloque a URL aqui.
    // Exemplo: socketServerUrl: 'https://emoji-wars-server.onrender.com'
    socketServerUrl: ''
};
